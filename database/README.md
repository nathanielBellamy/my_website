# Database Initialization

This directory contains the SQL script to initialize the PostgreSQL database for the `my_website` project.

## Usage

To initialize the database, you will first need to have the `psql` CLI tool installed and running locally on your machine.

Then, from the root of the project, simply run the following command:

```bash
./database/run-sql.sh
```
